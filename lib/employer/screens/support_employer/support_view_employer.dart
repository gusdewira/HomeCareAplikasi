import 'package:flutter/material.dart';

class SupportEmployerView extends StatelessWidget {
  const SupportEmployerView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}